import { RoutePlaceholder } from "@/components/dashboard/route-placeholder";

export default function ProxyPage() {
  return <RoutePlaceholder eyebrow="Proxy Lines" title="反代设置" description="管理个人反代线路，后续会接入线路列表、新增线路与删除线路能力。" />;
}
