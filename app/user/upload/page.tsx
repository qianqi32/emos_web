import { RoutePlaceholder } from "@/components/dashboard/route-placeholder";

export default function UploadPage() {
  return <RoutePlaceholder eyebrow="Upload" title="上传视频" description="用于视频与字幕上传流程，后续会接入上传令牌、基础信息匹配、进度跟踪与保存上传结果。" />;
}
